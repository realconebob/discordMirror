# Exclude anything in the debs folder
debs/*

# Exclude anything that should be in the debs folder but isn't for some reason
*.deb
Release
Packages*

# Exclude timestamp files generated by the cron helper
*.timestamp